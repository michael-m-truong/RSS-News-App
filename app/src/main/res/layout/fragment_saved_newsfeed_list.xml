<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/saved_news_feed_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" /> <!-- Initially set to gone -->

    <!-- Display text when no articles are saved -->
    <TextView
        android:id="@+id/no_saved_articles_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="?android:textColorPrimary"
        android:text="@string/No_saved_articles_dialogue"
        android:visibility="visible"/> <!-- Initially set to gone -->

</LinearLayout>